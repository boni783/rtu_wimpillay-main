name: rtu_wimpillay
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1        # ✅ NUEVO: Para autenticación
  google_sign_in: ^7.2.0      # ✅ NUEVO: Para login con Google
  cloud_firestore: ^6.0.3     # ✅ ACTUALIZADO: Versión compatible
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true